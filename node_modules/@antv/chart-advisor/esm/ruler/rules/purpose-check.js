import { CKBJson } from '@antv/ckb';
var Wiki = CKBJson('en-US', true);
var allChartTypes = Object.keys(Wiki);
export var purposeCheck = {
    id: 'purpose-check',
    type: 'HARD',
    docs: {
        lintText: 'Choose chart types that satisfy the purpose, if purpose is defined.',
    },
    trigger: function (_a) {
        var chartType = _a.chartType;
        return allChartTypes.includes(chartType);
    },
    validator: function (args) {
        var result = 0;
        var chartType = args.chartType, purpose = args.purpose;
        // if purpose is not defined
        if (!purpose) {
            result = 1;
            return result;
        }
        if (chartType && Wiki[chartType] && purpose) {
            var purp = Wiki[chartType].purpose || '';
            if (purp.includes(purpose)) {
                result = 1;
                return result;
            }
        }
        return result;
    },
};

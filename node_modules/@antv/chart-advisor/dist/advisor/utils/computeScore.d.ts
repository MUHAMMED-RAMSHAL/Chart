import { Info, RuleModule } from '../../ruler';
import type { ScoringResultForRule } from '../advice-pipeline/interface';
declare const computeScore: (ruleBase: Record<string, RuleModule>, ruleType: 'HARD' | 'SOFT', info: Info, log: ScoringResultForRule[]) => number;
export default computeScore;

import { ChartAntVSpec } from '../../schema';
export declare type G2PlotType = 'Line' | 'Area' | 'Column' | 'Bar' | 'Pie' | 'Rose' | 'Scatter' | 'Histogram' | 'Heatmap';
/**
 * get chart type (Line / Bar / ...) from spec's mark (bar / line / ...)
 * @param spec input spec
 * @returns chart type
 */
export declare function markToChart(spec: ChartAntVSpec): any;
/**
 * translate antv-spec to g2plot configuration
 * @param spec
 * @returns configuration to plot g2plot
 */
export declare function specToG2PlotConfig(spec: ChartAntVSpec): {
    chartType: string;
    config: Record<string, any>;
};

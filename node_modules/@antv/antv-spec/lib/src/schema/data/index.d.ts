import { DataRow } from './dataRow';
import { GraphData } from './graphData';
export declare type Data = GraphInlineData | GraphOnlineData | ChartInlineData | ChartOnlineData;
export declare type ChartDataDef = ChartInlineData | ChartOnlineData;
export declare type GraphDataDef = GraphInlineData | GraphOnlineData;
declare type DataConfig = {
    delimiter?: string;
};
export declare type GraphInlineData = {
    /**
     * type of `values`
     */
    type?: 'json';
    values: GraphData;
};
export declare type GraphOnlineData = {
    type?: 'url';
    /**
     * URL of the data source.
     */
    values: string;
    /**
     * type of the data, for correctly parsing
     */
    format?: 'json';
};
export declare type ChartInlineData = {
    type?: 'json-array';
    values: DataRow[];
};
export declare type ChartOnlineData = {
    type?: 'url';
    /**
     * URL of the data source.
     */
    values: string;
    /**
     * type of the data, for correctly parsing
     */
    format?: 'csv' | 'json';
    /**
     * config for parsing data
     */
    config?: DataConfig;
};
export { DataRow };

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const schema_1 = require("../src/schema");
describe('mark test', () => {
    test('chart mark type', () => {
        expect(schema_1.CHART_MARK_TYPES).toEqual(['bar', 'line', 'arc', 'area', 'point', 'rect']);
    });
    test('node mark type in graph', () => {
        expect(schema_1.NODE_MARK_TYPES).toEqual(['point', 'arc', 'rect']);
    });
    test('link mark tyoe in graph', () => {
        expect(schema_1.LINK_MARK_TYPES).toEqual(['line']);
    });
});
//# sourceMappingURL=mark.test.js.map
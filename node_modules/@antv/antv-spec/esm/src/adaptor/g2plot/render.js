import * as G2Plot from '@antv/g2plot';
export function g2plotRender(g2plotCfg, container) {
    const { chartType } = g2plotCfg;
    if (g2plotCfg && chartType) {
        const plot = new G2Plot[chartType](container, 
        // @ts-ignore
        g2plotCfg.config);
        plot.render();
        return plot;
    }
    return null;
}
//# sourceMappingURL=render.js.map
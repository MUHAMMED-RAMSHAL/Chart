import { CKBJson } from '@antv/ckb';
import Thumbnails, { BAR_CHART } from '../src/index';
test('index', function () {
    var ckb = CKBJson();
    var keys = Object.keys(Thumbnails);
    keys.forEach(function (key) {
        var obj = Thumbnails[key];
        expect(!!obj).toBe(true);
        if (obj) {
            expect(obj.id).toBe(key);
            expect(obj.name).toBe(ckb[key].name);
            expect(!obj.svgCode).toBe(false);
        }
    });
});
test('object pie', function () {
    if (Thumbnails.pie_chart) {
        var _a = Thumbnails.pie_chart, id = _a.id, name_1 = _a.name, svgCode = _a.svgCode;
        console.log(id, name_1, svgCode);
    }
});
test('object bar', function () {
    var id = BAR_CHART.id, name = BAR_CHART.name, svgCode = BAR_CHART.svgCode;
    console.log(id, name, svgCode);
});
//# sourceMappingURL=index.test.js.map
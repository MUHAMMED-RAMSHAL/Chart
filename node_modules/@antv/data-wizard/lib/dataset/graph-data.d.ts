import { GraphProps } from '../analyzer';
import { NodeData, LinkData, GraphInput, GraphExtra } from './types';
import DataFrame from './data-frame';
export default class GraphData {
    private extra;
    data: {
        nodes: NodeData[];
        links: LinkData[];
    };
    private autoParse;
    constructor(data: GraphInput, extra?: GraphExtra);
    getNodeFrame(): DataFrame;
    getLinkFrame(): DataFrame;
    /**
     * Get statistics.
     */
    info(): GraphProps;
}

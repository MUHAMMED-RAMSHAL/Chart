import * as React from 'react';
import { ThumbnailID } from '@antv/thumbnails';
export interface ThumbnailProps extends React.ImgHTMLAttributes<HTMLImageElement> {
    chart?: ThumbnailID;
    svg?: string;
}
export declare const Thumbnail: (props: ThumbnailProps) => JSX.Element;

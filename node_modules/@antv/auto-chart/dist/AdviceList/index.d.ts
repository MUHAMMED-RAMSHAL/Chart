/// <reference types="react" />
import { Language } from '../i18n';
import type { Advice } from '@antv/chart-advisor';
interface AdviceListProps {
    language: Language;
    advices: Advice[];
    currentIndex: number;
    isActive: boolean;
    onChartTypeChange: (valueIndex: number) => void;
}
export declare const AdviceList: ({ advices, currentIndex, language, isActive, onChartTypeChange }: AdviceListProps) => JSX.Element;
export {};
